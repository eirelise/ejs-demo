<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head'); %>
</head>
<body class="container">

<header>
  <%- include('../partials/header'); %>
</header>

<main>
  <div class="box">
      <h1>This is great</h1>
      <p>Welcome to templating using EJS</p>
      <h2>Variable</h2>
      <p><%= tagline %></p>
      </div>
    <article class="sideBar">
      <h2>reg users</h2>
  
        <ul class="userList">
          <% if (fafo.length === 0) { %>
            <li>No users found yet!</li>
          <% } else { %>
            <% for (let user of fafo) { %>
              <li class="userItem">
                <img src="<%= user.pfp || '/placeholder.png' %>" alt="Profile picture" width="32" style="border-radius: 50%;">
                
                <strong><%= user.username %></strong> 
                <span class="muted">Joined: <%= user.created_at %></span>
              </li>
            <% } %>
          <% } %>
        </ul>
    </article>

</div>
</main>

<footer>
  <%- include('../partials/footer'); %>
</footer>

</body>
</html>